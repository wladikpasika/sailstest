<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Библиотека</a>
    </div>

    <a href ='/User/registration'>Регистрация</a>

    <% if (session.authenticated) { %>
    <a href ="/user/show/<%= session.User.id %>">
        <span>Добро пожаловать, <%if (session.User.firstname) {%>

          <%= session.User.firstname+" "+session.User.lastname%>

          <%} else {%>
          <%= session.User.name%>

          <% } %> </span></a>
    <a href="/sessions/destroy">
      <button class="btn btn-danger navbar-btn">Выход</button></a>
    <%}

    else

    { %>
    <a href= '/sessions/new'>
      <button class="btn btn-danger navbar-btn">Авторизация</button></a>
    <% } %>
  </div>
</nav>
